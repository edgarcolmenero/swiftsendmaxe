import '@app/index';
import { initHeader } from '@widgets/Header';
import { initHero } from '@widgets/Hero';
import { initSmoothScroll } from '@features/smooth-scroll';
import { initActiveSectionLink } from '@features/active-section-link';

initHeader();
initHero();
initSmoothScroll();
initActiveSectionLink();
